# studio32-dashboard-deploy

> ⚠️ **Este repositorio contiene una build generada. No edites nada aquí a mano.**

Artefacto de despliegue de **dashboard.studio32.es**. Todo lo que hay dentro (`index.html`, `assets/`) lo produce el proceso de build a partir del código fuente.

- **Código fuente:** [`studio32-panel`](https://github.com/tsmluky/studio32-panel)
- **Dominio:** `dashboard.studio32.es` (ver `CNAME`)
- `.nojekyll` desactiva el procesado de GitHub Pages.

Cualquier cambio hecho directamente en este repo se perderá en el siguiente despliegue.
